import ContactPageClient from "@/components/support/ContactPageClient";

export const metadata = { title: "Contact — Stock Analyst" };

export default function ContactPage() {
  return <ContactPageClient />;
}

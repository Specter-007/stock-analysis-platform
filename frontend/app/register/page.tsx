import RegisterPageClient from "@/components/auth/RegisterPageClient";

export const metadata = {
  title: "Create account — Stock Analyst",
};

export default function RegisterPage() {
  return <RegisterPageClient />;
}

import ModelPageClient from "@/components/model/ModelPageClient";

export const metadata = {
  title: "Model — Stock Analyst",
};

export default function ModelPage() {
  return <ModelPageClient />;
}
